[project]
name = "pancratius"
version = "0.1.0"
description = "Python tooling for the Pancratius static library"
requires-python = ">=3.13"
dependencies = [
  "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
  "pytest>=9.0",
]

[tool.uv]
package = false
