{
  "//": "IDE discovery only: applies the strict Node tooling config to Playwright specs, unit tests, and visual QA helpers. The command-line check of record is `tsc -p tsconfig.scripts.json`.",
  "extends": "../tsconfig.scripts.json",
  "include": ["**/*.ts"]
}
