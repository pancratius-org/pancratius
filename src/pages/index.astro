---
// Apex redirect stub. `i18n.redirectToDefaultLocale` requires a root `/` route
// and bakes this `/ → /<default>/` redirect (meta-refresh) as the universal
// fallback; a true 301 is added host-side (beget `.htaccess`, Cloudflare zone
// rule). See docs/architecture.md.
---
