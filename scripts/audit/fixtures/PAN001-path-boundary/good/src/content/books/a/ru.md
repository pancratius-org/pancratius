---
title: Good fixture (FP regression — must stay silent)
lang: ru
---

Dialogue notation contains the substring `:\]`, the Windows-drive trap:

*\[The Guide, who has been silent all this time, says softly:\]*

The bare English word legacy appears in prose with no following slash, so it is
not a path reference — this work is part of his early legacy.

A legitimate relative asset reference resolves inside the bundle:

![](./images/x.jpg)

An external reference is allowed:

See [the source](https://example.com/path) for context.

A bare drive letter with no backslash path is just prose: the disk labelled C:
was full when he wrote this.

An in-bundle parent reference stays inside the content root, so it is allowed:

![sibling](../b/images/shared.jpg)
