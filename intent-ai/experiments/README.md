# Experiment evidence

Top-level directories are evidence produced against the current source identity. Historical
results live under `history/<source-version>/`; they remain auditable but are not runtime inputs.

Experiments are append-only. Re-run a question under a new identity when source, truth, or producer
semantics change.
