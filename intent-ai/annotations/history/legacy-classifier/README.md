# Legacy classifier adjudications

`raw/` contains the six human-adjudication files named by active label provenance. They are
immutable evidence, not loader inputs. The scratch readers, generated assessments, duplicate UI,
and one-off feature pipelines that produced the legacy corpus were retired when `intent-ai` became
the canonical research package.
