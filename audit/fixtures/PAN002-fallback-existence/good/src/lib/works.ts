// Fixture stub — just enough for PAN002's derive-guard: it must find an exported
// function named `displayWorkEntry` here (the display-fallback selector). No real
// logic needed.

/** Display-fallback selector (stub). */
export function displayWorkEntry(): unknown {
  return null;
}
