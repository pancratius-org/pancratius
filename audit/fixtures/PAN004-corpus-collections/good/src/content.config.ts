// Fixture (PAN004-corpus-collections / good): the full collection set. The rule
// only reads the property-name KEYS of this object literal, so the values can be
// bare stubs — no real defineCollection needed.
const books = {};
const poetry = {};
const projects = {};
const pages = {};

export const collections = { books, poetry, projects, pages };
