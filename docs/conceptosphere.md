# Conceptosphere Data Stack

The concept/book sphere is a corpus map for Pancratius: books, poems, and
projects, currently from the Russian Markdown content.

It has three jobs:

| Surface | User question | Method |
| --- | --- | --- |
| **Концепты** | What ideas recur, and how do they relate? | Lemma co-occurrence graph, NPMI pruning, Leiden clusters |
| **Книги** | Which books belong near each other? | Book vectors over key concepts, TF-IDF cosine, Leiden clusters |
| **Похожие книги** | What should I read after this book? | One merged recommendation ranker using TF-IDF concept overlap and Qwen3 embedding similarity |

The embedding graph is not shown as a third public graph. Its value is in
recommendations: it catches related books whose vocabulary does not overlap
enough for TF-IDF.

On book pages, recommendations are a single reader-facing list. Do not show a
"TF-IDF list" beside an "embedding list" in the reading UI. Merge both signals,
mark convergent items with `★`, and exclude works already shown in authored
`cross_refs` / **См. также**. The graph explorer may expose more analytical
detail because analysis is the point of that page.

## Generated Data Products

```
data/
  pancratius-concepts-graph.json  source artifact for the public concepts graph
  pancratius-books-graph.json     source artifact for the public books graph,
                                  including top_concepts, top_similar,
                                  top_similar_embed
  conceptosphere-embed.json       embedding intermediate; not a public UI payload
  conceptosphere-embed-cache/     local embedding cache; do not deploy

scripts/
  conceptosphere.py               concepts graph + books graph generator
  conceptosphere_embed.py         semantic embedding generator

design/
  conceptosphere.html             current standalone prototype
```

This document defines the generated graph artifacts and the science behind them. The site architecture decides whether publishable artifacts are copied, bundled, or emitted into the final static build — see [`architecture.md`](./architecture.md).

## Example Output Snapshot

These numbers are a sanity snapshot from the generated files in this checkout,
not a stable contract. They are useful for spotting accidental graph-shape
changes after a rebuild; regenerate the JSON if you need fresh counts.

| Output | Nodes | Edges | Clusters | Modularity |
| --- | ---: | ---: | ---: | ---: |
| Concepts | 418 | 1,214 | 20 | 0.7210 |
| Books, TF-IDF | 72 | 330 | 6 | 0.3424 |
| Embedding intermediate | 117 | 851 | 4 | 0.3847 |

Embedding intermediate also contains 14,880 chunks and 12 discovered chunk
topics. It includes books, poems, and projects; semantic recommendations in
the public books graph carry `kind` so the UI can link each item correctly.

## Concepts Graph

Generated by:

```sh
uv run scripts/conceptosphere.py --mode concepts
```

Pipeline:

1. Read `src/content/{books,poetry,projects}/*/ru.md`.
2. Strip frontmatter and Markdown structure.
3. Tokenize and lemmatize Russian with `pymorphy3`.
4. Drop stopwords, filler, weak parts of speech, and corpus-specific noise.
5. Build lemma co-occurrence edges in a 4-token window.
6. Prune by `NPMI >= 0.18`, raw weight `>= 6`, top 10 edges per node, minimum frequency 14, and final cap 420 nodes.
7. Use weighted PageRank for node size.
8. Use Leiden for clusters; Louvain is still computed as a sanity comparison.

Why this shape: co-occurrence is the right level for a public idea map. It is
not trying to prove theology or infer author intent; it shows what terms
reliably travel together in the corpus.

## Books Graph

Generated by:

```sh
uv run scripts/conceptosphere.py --mode books
```

Pipeline:

1. Reuse the processed concept counts.
2. Keep books only.
3. Build per-book TF-IDF vectors over concepts.
4. Connect books by cosine similarity.
5. Keep the top 5 neighbors per book, with a low cosine floor of 0.10.
6. Use weighted PageRank for node size.
7. Use Leiden for clusters.
8. Attach `top_concepts`, `top_similar`, and, if available, `top_similar_embed`.

Why this shape: raw overlap and Jaccard over-reward long books. TF-IDF cosine
keeps a short book and a long book comparable while preserving the editorial
question: which books share conceptual vocabulary?

## Semantic Recommendations

Generated by:

```sh
uv run scripts/conceptosphere_embed.py
uv run scripts/conceptosphere.py --mode books
```

Run the books graph after the embedding pipeline if you want fresh semantic
recommendations in `pancratius-books-graph.json`.

Embedding pipeline:

1. Read the same Russian corpus.
2. Split texts into roughly 400-token chunks with 50-token overlap.
3. Embed chunks with `mlx-community/Qwen3-Embedding-0.6B-bf16` by default.
4. Apply the same instruction prefix to every chunk because this is symmetric
   similarity/clustering, not asymmetric search.
5. Mean-center all chunk embeddings before book centroids. This removes the
   common "all these texts are from the same spiritual corpus" component.
6. Build length-weighted document centroids.
7. Connect documents by centroid cosine using top-K plus threshold pruning.
8. Discover chunk topics with HDBSCAN.
9. Emit `most_similar`; the books graph generator inlines the top five as
   `top_similar_embed`.

Why this shape: embeddings are strongest where vocabulary overlap is weakest.
They are useful for "read next" recommendations, but using them as a competing
public graph makes the product harder to understand.

## Book-Page Recommendation Contract

Book pages have two distinct relation surfaces:

- **См. также** — authored references from work frontmatter `cross_refs`.
- **Похожие книги** — algorithmic suggestions from `pancratius-books-graph.json`.

The recommendation component merges `top_similar` (TF-IDF concepts) and
`top_similar_embed` (semantic embeddings) into one ranked list. Treat this as a
small ranker, not a concatenation. A simple implementation is still fine, but it
must normalize both source lists, cap the convergence boost so "present in both"
does not automatically beat a very strong single-source match, and keep ordering
stable. Mark convergent items with `★`. Exclude the current work and anything
already rendered in **См. также**.

Long catalog/bibliography snapshots are not recommendation inputs. They live in
`bibliography.yaml` and are only provenance/export material.

## Rebuild Order

Public graph refresh without embeddings:

```sh
uv run scripts/conceptosphere.py --mode concepts
uv run scripts/conceptosphere.py --mode books
```

Full refresh with semantic recommendations:

```sh
uv run scripts/conceptosphere_embed.py
uv run scripts/conceptosphere.py --mode concepts
uv run scripts/conceptosphere.py --mode books
```

Smoke test embedding generation:

```sh
uv run scripts/conceptosphere_embed.py --limit 5
```

The concept generator default is `--mode concepts`; it does not build both
public graph files in one invocation.

## Design Decisions

| Decision | Choice | Reason |
| --- | --- | --- |
| Clustering | Leiden | Better community quality and connectedness guarantees than Louvain; Louvain stays as a comparison metric |
| Concept centrality | PageRank | Sizes "gravity well" concepts better than betweenness for this public map |
| Book edge weight | TF-IDF cosine | Normalizes book length and preserves conceptual overlap |
| Semantic model | Qwen3-Embedding-0.6B via MLX | Good Russian coverage, cheap enough locally; 4B is the obvious production upgrade |
| Mean-centering | On | Prevents homogeneous-corpus cosine collapse |
| Public graph UI | Two graphs, not three | Concepts and books are distinct user questions; embeddings are a recommendation signal |
| Book-page recommendations | One merged list | Readers need one "what next" surface; convergence gets a `★` instead of a second column |

## Known Gaps

- The graph generators are Russian-only today (`ru.md` discovery is hardcoded).
- `conceptosphere-embed.json` includes poems/projects, so UI recommendations must
  respect `kind` and not assume every semantic neighbor is a book.
- Topic labels in the embedding intermediate can still leak stopwords or HTML-ish
  tokens; they are not a production UI surface yet.
